<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/helper_main_drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <!-- Framelayout to display Fragments -->
    <FrameLayout
            android:id="@+id/frame_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    <!--
        android:divider="@android:color/transparent"
    -->
    <!-- Listview to display slider menu -->
    <ListView
            android:id="@+id/helper_ban_aside_list_slidermenu"
            android:layout_width="240dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:choiceMode="singleChoice"
            android:divider="@color/helper_nav_aside_divider_dark"
            android:dividerHeight="1dp"
            android:listSelector="@drawable/nav_list_selector"
            android:background="@color/helper_nav_aside_bg_dark"/>
</android.support.v4.widget.DrawerLayout>